file:
  extension:
    policy:
      fixed-extensions:
        - bat
        - cmd
        - com
        - cpl
        - exe
        - scr
        - js
      max-custom-extensions: 200
      max-extension-length: 20

logging:
  file:
    name: logs/file-extension-blocker.log
  level:
    org.hibernate.orm.jdbc.bind: trace
    org.hibernate.SQL: debug
    root: info

server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /

spring:
  application:
    name: file-extension-blocker

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://13.209.67.184:3306/file_ext_blocker?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "Rlaalsgml312!"

  devtools:
    livereload:
      enabled: true

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/

  servlet:
    multipart:
      location: uploads/
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    time-zone: Asia/Seoul
    date-format: yyyy-MM-dd HH:mm:ss